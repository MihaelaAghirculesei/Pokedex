# Sentry error monitoring (https://sentry.io → create project → get DSN)
# Copy this file to .env.local and fill in your values.
# .env.local is gitignored — never commit real DSNs.
VITE_SENTRY_DSN=

# Optional: override the release tag sent to Sentry (defaults to "1.0.0")
VITE_APP_VERSION=1.0.0
